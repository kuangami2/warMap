# 秦统一至汉初 · 历史战争地图项目手册

**版本：** v2.6.0（来源证据台与本地研究收藏）
**专题范围：** `qin-han` 前 230 年至前 180 年；`han-three-kingdoms` 前 202 年至 280 年
**项目目录：** `D:\warMap`

**2.0 候选定位：** 专业自然地理＋历史地点＋势力范围＋战争叙事地图；当前不含、也不声称有精确历史行政区面。

---

## 1. 项目要解决什么问题

本项目不是一张静态的中国地图，也不只是按年份罗列战役。它要让用户通过拖动时间轴，直观看到以下历史过程如何在空间上展开：

1. 秦如何完成统一；
2. 秦帝国如何迅速陷入崩解；
3. 项羽、刘邦等势力如何争夺新秩序；
4. 汉初如何从战争中重建政治格局；
5. 两汉帝国如何经营边疆、经历危机，并走向汉末、三国与西晋统一。

最终产品是一个无需登录、可在电脑和手机浏览的交互式网站。地图上的节点、路线、云团和详情面板必须随年份同步变化，并为关键史料提供来源和可信度说明。

---

## 2. 当前状态：第一轮已完成什么，尚缺什么

### 已完成

- 项目已使用 Next.js、TypeScript 和 Tailwind CSS 初始化；
- 已有可启动的单页网页；
- 已实现前 230 年至前 180 年的时间轴、叙事播放/暂停、速度控制和阶段跳转；
- 已实现 45 个结构化事件，覆盖秦灭六国、帝国边疆经营、秦末起义、楚汉战争和汉初重建；
- 已将势力图层扩展为 26 个历史快照，区分核心控制、主要影响、争夺区和活动范围；
- 已建立专题化数据读取接口；当前专题为“秦统一至汉初”，2.0 预留给“汉代＋三国地图”；
- 已实现事件筛选、地图节点、右侧统计和详情面板；
- 每条现有事件均含基本叙事、来源字段及可信度；
- 已以本地裁切的 Natural Earth 1:10m 物理矢量数据替换 1:110m 中国轮廓；海岸、湖泊和河流保留现代地理参考属性，未将其伪装为秦汉政区；
- 已实现总览、区域、局部三档信息密度，地理、地名、热区与事件按同一比例尺规则渐进展开；
- 已实现 16 节来源可追溯的战争叙事播放：按事件聚焦、路线/战场高亮、摘要/可信度/来源卡片、前后节控制及可恢复的分享链接；
- 已加入时间逻辑与事件完整性单元测试，并确认生产构建通过。

### 还未完成：你指出的三个核心问题

| 问题 | 当前真实状态 | 后续处理方式 |
|---|---|---|
| 网页端地图尚未生成 | **已解决基础版本。** 当前为本地打包的 Natural Earth 中国现代地理轮廓图，可正确投影事件位置；仍未复原秦汉历史政区和古河道。 | 第四轮继续审校历史地理表达。 |
| 战争事件数量较少 | **已在 1.2 深化。** 当前有 45 条结构化事件，并区分战争进程、战役、围城、起义、政治转折和外交。 | 后续继续审校来源与路线。 |
| 公网部署 | v2.6.0 本地构建已通过；Vercel 生产部署需在具备 Vercel CLI 权限的环境执行。 | GitHub Pages 静态包已生成；本次会话推送被 GitHub 凭据阻断。 |

### 当前网站不是“未生成网页”

网页本身已经生成并通过生产构建；需要准确区分以下两件事：

- **已完成：** 可在本机运行的网页、本地真实地理轮廓参考图、时间轴与 45 条结构化事件；
- **已完成：** 云团、节点、10 条路线、图例、动效开关、移动端、数据校验与本地生产验收；
- **尚未完成：** 更精细的秦汉历史政区复原，以及需要外部账号授权的公网发布。

---

## 3. 五轮开发计划

### 第一轮：可运行的交互骨架（已完成）

目标是验证产品的基本交互链路，而不是立刻追求史料规模或地图精度。

- 完成网页框架、暗色视觉、时间轴和阶段切换；
- 建立 `WarEvent`、`Location`、`Participant`、`Source` 等数据模型；
- 录入 5 条代表性事件；
- 实现事件筛选、节点点击和详情显示；
- 完成单元测试与生产构建。

验收结果：已通过。当前可运行，也可继续扩展。

### 第二轮：真实底图与完整历史数据（已完成 MVP）

这是下一轮的重点，解决“地图还不是真实地图、战争太少”两个核心问题。

#### 地图工作

- 选择并引入可合法使用、可本地保存的中国区域 GeoJSON 或 SVG 底图；
- 设计历史地图视觉层：古风暗色底图、河流、主要区域、核心地名；
- 不把现代省界伪装成秦汉行政边界；如需现代地名，仅作为辅助定位信息；
- 保留投影和图层抽象，使后续可替换更准确的数据资产；
- 支持地图缩放、平移或至少保证桌面端与手机端的可读性。

#### 数据工作

将事件数从 5 条增加到 **不少于 30 条**。建议先按六个内容阶段录入：

| 阶段 | 目标事件示例 | 建议数量 |
|---|---|---:|
| A. 秦统一战争 | 秦灭韩、赵、魏、楚、燕、齐 | 6 |
| B. 帝国建立与扩张 | 北击匈奴、南征百越、岭南经营等 | 4–5 |
| C. 秦末起义 | 大泽乡起义、陈胜吴广扩张、项梁项羽起兵、刘邦起兵、巨鹿、入关等 | 7–8 |
| D. 楚汉战争前期 | 鸿门宴前后、分封、刘邦入汉中、三秦战争、彭城之战、韩信北伐等 | 6–7 |
| E. 楚汉战争结局 | 井陉之战、潍水之战、垓下之战、项羽败亡等 | 4–5 |
| F. 汉初重建 | 异姓诸侯冲突、英布之乱、北方边疆与南越关系等 | 4–5 |

每条事件的最低数据要求：

- 事件名、开始/结束年份、事件类型、规模等级；
- 至少一个地理位置；
- 参与方、背景、结果、影响；
- 至少一个史料或现代参考来源；
- `high`、`medium` 或 `low` 的可信度；
- 兵力、伤亡、具体地点有争议时，使用区间或说明文字，不能伪造成精确数字。

第二轮验收结果：已达到 MVP 标准。地图已是可读的中国现代地理底图参考，事件均能随时间轴、地图节点、统计面板和详情面板联动。

### 第三轮：战争云团、行军路线与叙事体验（已完成）

目标是从“可点击的事件点”升级为“能读出历史过程的地图”。

- 实现 `WarCloudLayer`，按照时间窗口和地理邻近度聚合战争活动；
- 云团强度综合事件数量、规模和持续时间，但不能让用户误以为云团面积等于伤亡规模；
- 支持“云团 / 节点 / 关闭动画”三种显示模式；
- 为至少 8 条事件补充路线或多地点叙事，例如秦灭楚、刘邦入关、韩信北伐、垓下合围；
- 选中事件后只显示其相关路线，避免全图线条堆叠；
- 在页面上增加阶段一句话叙事、图例和云团说明；
- 增加低动效模式，尊重系统的“减少动态效果”设置。

第三轮验收结果：已实现云团/节点/综合模式、动效开关、图例、阶段叙事以及 10 个事件的路线。密集年份标签改为悬停、聚焦或选中时显示。

### 第四轮：质量、移动端和历史审校（已完成本地验收）

目标是把原型打磨成稳定、可长期维护的公开网站。

- 补充数据验证：年份范围、坐标合法性、来源字段、跨年事件、缺失路线、重叠地点；
- 完善单元测试和组件交互测试；
- 处理同地点多个事件的标签避让、抽屉滚动和小屏幕窄屏布局；
- 完成键盘操作、焦点状态、按钮说明和基本无障碍支持；
- 检查字体、对比度、地图标注和移动端触控可用性；
- 核查高争议数据，标注地点异说、兵力异说和史料局限；
- 更新 README、数据说明和已知限制文档。

第四轮验收结果：桌面和 390px 移动端均完成截图回归；数据校验覆盖年份、坐标、来源、地点、重复 ID 与路线点数；5/5 自动测试通过。

### 第五轮：上线部署与发布验证（部署准备完成，公网授权待办）

目标是让网站拥有稳定的公网访问地址。

推荐优先采用 **Vercel** 部署，因为它对 Next.js 的静态/服务端构建支持最直接。也可以根据你已有的账号、域名或服务器改用 Cloudflare Pages、Netlify、GitHub Pages（仅适合纯静态导出）或自己的服务器。

部署流程：

1. 创建或确认代码托管仓库；
2. 将项目推送到仓库；
3. 在部署平台连接仓库；
4. 设置构建命令为 `npm run build`；
5. 设置部署平台所需的 Node.js 版本为 20；
6. 生成预览地址并测试；
7. 如有域名，再配置自定义域名与 HTTPS；
8. 在手机网络和桌面浏览器上做一次发布后回归测试。

第五轮当前结果：`vercel.json`、Node.js 20 配置和部署手册已经完成，生产构建成功。公开 HTTPS 地址仍需要 Vercel、GitHub 或其他平台账号授权，因此不在当前无登录环境中冒充已上线。

---

## 4. 需要持续解决的问题清单

以下清单包含你提出的问题，也加入了项目长期可用所必须处理的问题。它们会按上述五轮逐步完成。

### P0：必须解决，影响项目是否成立

- [x] 用真实、可审校的中国地理底图替换当前抽象 SVG；
- [x] 将战争/战役事件补充至至少 30 条；
- [ ] 为所有新增事件提供来源和可信度；
- [x] 实现战争云团、事件节点和路线三种互补表达；
- [ ] 完成公网部署，生成稳定访问地址；
- [ ] 验证公开网站在手机端可正常操作。

### P1：高优先级，影响内容可信度和使用体验

- [ ] 审校所有事件的年份、地点、参与方和阶段归属；
- [ ] 对历史地点坐标与现代定位不完全一致的情况做说明；
- [ ] 不将现代省界作为秦汉行政区划；
- [ ] 为至少 8 个事件提供路线/多地点数据；
- [ ] 解决事件节点或标签在同一地点重叠的问题；
- [ ] 补全空年份的叙事提示，避免用户误以为数据加载失败；
- [ ] 加入来源说明页，区分正史记载、后世考证和现代研究资料。

### P2：发布前建议完成，影响专业性和维护能力

- [x] 增加地图图例与“云团不是伤亡规模”的说明；
- [x] 增加低动效/关闭动画开关；
- [x] 添加键盘可操作性、焦点样式和屏幕阅读器标签；
- [x] 建立数据校验脚本，防止无来源、无坐标或日期越界的数据进入页面；
- [ ] 在 README 中记录数据来源、许可、地图资产来源和已知限制；
- [ ] 增加分析工具或匿名访问统计前，先明确隐私策略；
- [ ] 处理首次加载性能和地图资源体积。

### 需要你最终决定的外部事项

以下事项涉及外部账号、公开发布或花费，不能默认替你决定：

| 事项 | 推荐默认值 | 需要你的决定 |
|---|---|---|
| 部署平台 | Vercel | 是否使用已有 Vercel/Cloudflare/其他平台账号。 |
| 代码托管 | GitHub 私有或公开仓库 | 是否公开源代码和历史数据。 |
| 自定义域名 | 可暂不配置 | 是否已有域名，是否要绑定。 |
| 地图资产来源 | 本地开源/可用许可 GeoJSON | 是否采用指定数据源或由项目选择合规公开数据。 |
| 发布范围 | 先用预览地址验收，再公开 | 是否需要公开传播、备案、统计或访问权限限制。 |

---

## 5. 本地运行与验证

安装依赖：

```powershell
cd D:\warMap
npm install
```

启动本地开发服务：

```powershell
npm run dev
```

打开：`http://localhost:3000`

运行测试：

```powershell
npm test
```

生成生产构建：

```powershell
npm run build
```

当前验证记录：

- `npm test`：通过，26 项测试通过；静态输出预算检查通过；
- `npm run build`：通过，生产构建成功；
- 桌面总览、路线详情和 390px 移动端截图已完成视觉回归；
- 当前没有外部地图服务密钥、环境变量或运行时数据库依赖；v1.4 继续通过专题化仓储接口、稳定 ID、显式势力归属、关系校验与地图视口逻辑维护数据边界。是否引入数据库将在需要后台编辑、多人协作或 2.0 大规模内容扩展时重新评估。

---

## 6. 当前项目目录说明

```text
D:\warMap
├─ app/                 # 页面入口和全局样式
├─ components/          # 地图、时间轴、统计、详情组件
├─ data/                # 战争事件与叙事阶段数据
├─ lib/                 # 类型定义、年份和筛选逻辑
├─ tests/               # 单元测试
├─ README.md            # 快速启动与当前版本说明
├─ PROJECT_MANUAL.md    # 本手册
└─ package.json         # 脚本与依赖
```

---

## 7. 执行原则

1. 先保证历史叙事主线成立，再追求覆盖全部细节；
2. 历史数据不确定时明确写出不确定性，而不是补造精确数字；
3. 地图云团表达的是活动密度与叙事强度，不是人员伤亡的直接可视化；
4. 不依赖必须付费或必须暴露密钥的地图服务，除非后续明确选择；
5. 每一轮结束都要同时完成可运行验证、测试和文档更新；
6. 线上发布前必须完成移动端检查、来源检查与性能检查。

---

## 8. 2026-08-10 上线与工程交付更新

- 第五轮公网部署已经完成，生产地址为 https://war-map-sage.vercel.app；
- GitHub 仓库为 https://github.com/kuangami2/warMap；
- Vercel 项目为 `shining3/war-map`；
- 项目已升级到 Next.js 16.3.0，部署运行时声明为 Node.js 24.x；
- lint、类型检查、5/5 自动测试和生产构建均已通过；
- npm 安全审计为 0 个已知漏洞；
- `.vercelignore` 已排除依赖、缓存、构建产物和截图，解决 CLI 上传体积过大的问题；
- 手动生产部署已经可用；GitHub 自动部署仍需项目所有者在网页端授权 Vercel GitHub App 访问 `kuangami2/warMap`；
- 完整的所有者操作、发布、回滚和故障排查说明见 `USER_DEPLOYMENT_GUIDE.md`。

当前秦统一至汉初专题已达到可公开访问的 MVP 状态。后续重点从“能否上线”转为史料审校、历史政权范围图层、按专题加载、三国专题扩展、端到端测试和长期内容维护。

---

## 9. 2026-08-10 国内可达性与移动端优化

- 手机端地图图例默认收起为小型按钮，支持关闭按钮、Escape 和地图空白关闭；
- 手机端在地图上方提供首屏可见的年份、播放和进度控制，倍速与历史阶段收进“更多控制”；
- 控制栏在正常状态下随滚动粘附，展开更多控制时回到文档流，避免遮挡大面积内容；
- 建立微信 320px、Android Chrome 375px、iPhone WebKit 390/414px、桌面 1280/1600px 的自动化交互和视觉矩阵；
- Vercel 国际主站继续使用 https://war-map-sage.vercel.app；
- GitHub Pages 试验镜像已发布到 https://kuangami2.github.io/warMap/；
- 镜像支持 `/warMap` 子路径静态资源，首页和关键 CSS 均返回 HTTP 200；
- 香港、东京外部探测和本机 Chromium/WebKit 测试通过，但 GitHub Pages 仍不能等同于已备案的国内 CDN；
- 由于现有 GitHub Token 缺少 `workflow` scope，Pages 使用 `npm run deploy:pages` 半自动发布，不要求用户扩大 Token 权限。

---

## 10. v1.3 势力区与标注收口更新

- 势力区标签撤回隶书，恢复 `Noto Serif SC`、`Microsoft YaHei` 和系统衬线字体；
- 标签按选中势力、核心控制区、主要影响区、争夺区和活动范围排序，并使用稳定的候选位置进行避让；
- 移动端只显示核心势力和当前事件关联势力标签，降低窄屏地图的标注密度；
- 事件节点和行军路线保持在势力区上层，选中事件仍会强化关联势力并降低其他势力透明度；
- 关键事件参与方使用显式 `polityId`，未能可靠归属的地方势力保留为空，不强行推断；

---

## 11. v1.4 移动地图视口更新

- 地图采用统一的缩放与平移视口模型：手机支持单指拖动、双指捏合和双击放大，桌面支持鼠标拖动与滚轮缩放；
- 缩放限制在 85%–300%，并以中国陆地区域约束平移边界，避免拖入无意义的大面积空白；
- 事件节点、事件名称、势力标签和台湾标注采用反向缩放，地图放大时维持稳定屏幕尺寸与触控可读性；
- 新增视口单元测试，以及 Android、微信和桌面浏览器矩阵中的捏合与反向缩放回归检查；本机 WebKit 运行时仍会在基础设施诊断后安全跳过。

---

## 12. v1.5 专题化地形底图更新

- 新增可开关的“地形”图层，将主要水系、专题相关山系、标签和古风山形符号从地图交互组件中拆分为独立、可维护的矢量资产；
- 水系与山系只作自然地理辅助定位，站内明确声明其不是对秦汉逐段河道或精确地形边界的复原；
- 所有地图资产在 `data/mapFeatures.ts` 集中记录名称、用途和许可，避免以无明确出处的网络地图图片充当事实底图；
- 新增地理要素边界、来源元数据和图层开关的自动校验。

---

## 13. v1.6 郡县、古地名与历史行政参考层

- 新增按年代生效的 `HistoricalPlace` 数据模型，统一维护郡国、县治、城邑与关隘的古名、今址、年代、来源、可信度与表达说明；
- 首批 20 个参考点覆盖秦统一至汉初专题的重要都城、郡国治所、战场城邑和关隘；全国视图优先显示高等级节点，放大后显示县邑；
- 地点图层仅以点和标签辅助历史定位，不绘制或暗示精确行政边界；按缩放后屏幕位置作碰撞筛选，避免密集地区标签重叠；
- 数据仓储、验证器和浏览器矩阵均已接入地点图层，确保年代切换、图层开关、来源信息和地图范围可持续验证。

---

## 14. v1.7 叙事地图导航、搜索与分享

- 新增八个历史区域快捷定位；地图可一键聚焦关中、中原、河北、齐鲁、江淮、巴蜀、岭南和河套；
- 新增统一搜索入口，支持按历史事件、古地名、现代地名与区域定位；选择事件时会同步时间轴、详情抽屉与地图视角；
- 将年份、选中事件、缩放与平移状态写入页面链接，支持复制和恢复同一地图观察视角；
- 地图视口由页面统一控制，外部导航、URL 恢复、按钮操作、鼠标与触摸手势均使用同一套边界约束；新增搜索、区域聚焦与 URL 状态浏览器回归。

---

## 15. v1.8 静态部署与性能门槛

- 新增静态部署预算检查，限制静态输出总量、所有 JavaScript gzip 合计和单个资源体积，避免专题扩展后手机首屏资源无控制膨胀；
- 检查静态 HTML 中是否存在外部运行时资源，确保地图和历史数据继续不依赖地图 API、SDK、密钥或第三方瓦片；
- 静态导出命令和 GitHub Pages 发布脚本均强制运行预算检查；Vercel 主站继续保持 Next.js 标准生产构建，GitHub Pages 保持纯静态镜像；
- 当前构建基线为约 0.90 MiB 静态输出、约 245 KiB gzip JavaScript，满足 v1.x 扩展前的性能余量。

---

## 16. v1.9 历史审校、跨端验收与 2.0 数据契约

- 强化战争、势力范围和历史地点的数据验证：来源标题、可信度、关键叙事字段、标签、古今地点说明和地图坐标均成为发布前检查项；
- 新增 `DATA_CONTRACT.md`，冻结 2.0 “两汉＋三国”专题的稳定 ID、证据与不确定性表达、行政点位规则、地图资产许可和发布门槛；
- 浏览器矩阵增加分享链接恢复验证，确保带年份、事件和地图视角的 URL 能在重新打开后正确恢复；
- 本机 Chromium、微信、Android 与桌面回归通过；本机 WebKit 自动化仍是已记录的基础设施限制，2.0 上线前需在真实 iPhone WebKit 环境补做验收。
- 当时间窗口没有事件时，地图、叙事条和统计面板均显示明确空状态，同时保留可用的历史势力范围；
- 详情抽屉增加 Tab 焦点循环、Escape 关闭和关闭后的焦点恢复；
- 当前数据仍是历史地理概括表达，势力区、路线和地点均不应理解为精确现代行政边界或道路复原。
