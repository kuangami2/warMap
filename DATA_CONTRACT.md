# 历史地图数据契约（v2.2.0 静态内容包基线）

本文件定义 2.x “两汉＋三国”专题沿用的数据最低标准。它不要求把有争议的历史地理伪装为精确事实；相反，所有不确定性必须成为可显示、可验证的数据。

## v2.1 / v2.2 内容包补充规则

- 每个专题内容包必须声明内容版本、编辑状态、研究区域、覆盖窗口和覆盖说明；研究区域仅用于检索与叙事分组，不表示行政区或国界。
- 发布物中的来源会归一为稳定来源 ID；事件、地点、势力快照和叙事引用必须可在专题来源目录中解析。
- 中低可信度事件必须保留地望、路线、规模或史料异说的限定说明。时间窗口须明确区分“已审校但无单列事件”与“未进入审校覆盖”。
- 行政地理数据集是独立的、默认禁用的未来接口。未取得公开展示与静态再分发授权前，不得注册行政区面或改变势力示意面的含义。

## 专题与稳定标识

- 每个专题、事件、势力快照、地点和地图资产使用稳定、唯一的英文 `id`；已发布 ID 不复用。
- 每条记录必须声明 `scenarioId` 与有效年代范围；现有专题分别为前 230 年至前 180 年（`qin-han`）和前 202 年至 280 年（`han-three-kingdoms`）。记录不得跨专题引用 ID。
- 事件、地点和路线坐标限定在中国专题地图支持范围内；超出范围的边疆资料须先扩展投影与审校规则。路线只能表达有来源支撑的概括行动方向，不能冒充精确行军轨迹。

## 证据与不确定性

- 战争事件、势力范围和历史地点均至少包含一条具标题的来源；地图资产记录名称、许可与使用范围。
- 每项历史结论均使用 `high`、`medium` 或 `low` 可信度。低可信度内容必须附有说明，不能通过实线边界、精确数值或确定措辞误导用户。
- 势力范围只能表达核心控制、影响、争夺或活动范围，不能冒充逐县行政边界。
- 郡国、县治、城邑和关隘默认为点位参考；除非 2.0 为某一时期取得并审校可分发的边界数据，否则不渲染行政区多边形。

## 2.0 最低数据要求

- 事件：名称、年代、类型、叙事四字段、参与方、至少一个具有古今说明的地点、标签、来源、可信度；路线至少两个点。
- 历史地点：古名、现代对应、类别、坐标、适用年代、显示层级、优先级、来源、可信度和地望说明。
- 地图资产：本地可分发、无运行时外部依赖；来源与许可可在站内说明中追溯。
- 战争叙事：每个叙事节必须稳定指向一个已审校的事件 `id`，运行时复用其年代、摘要、来源与可信度；叙事顺序属于编辑索引，不得以补写的剧情替代史料字段。

## 发布门槛

- 通过数据验证、类型检查、静态导出、静态预算检查和浏览器矩阵。
- 分享链接必须能恢复年份、选中事件、地图视角和（如有）叙事节；恢复叙事节不得自动开始播放。
- 无法在本机稳定运行的浏览器引擎必须明确记录为基础设施限制，不得声称通过。

## v1.9.8 / 2.0 候选边界

- 2.0 候选产品定位为“专业自然地理＋历史地点＋势力范围＋战争叙事地图”。自然地理、现代参考点与古地名点都不得被描述为精确历史行政区面。
- 当前历史 GIS 授权闸门为关闭状态：已核对的 CHGIS 许可禁止再分发，未纳入任何来源不明或仅限研究/个人使用的边界资产。没有新的、明确允许公开展示和静态再分发的授权包时，不得添加行政区多边形。
- Natural Earth 1:50m Populated Places 仅作为公共领域的现代地名参考层；必须带来源、版本、许可、覆盖和“非行政边界”处理说明。
- 发布前运行 `npm run audit:release-candidate`，并同时通过单元测试、类型检查、静态构建、静态预算与浏览器矩阵。该审计只能证明本地发布候选完整，不代表已部署或已完成真实设备验收。
