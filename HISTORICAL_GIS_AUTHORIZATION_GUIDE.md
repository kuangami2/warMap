# v1.9.7 历史 GIS 授权包交接与行政地理审校指导手册

本手册说明 v1.9.7 中哪些工作由 Codex 完成、哪些事项必须由项目所有者完成，以及如何准备和交付可公开展示、可随静态网站再分发的历史 GIS 数据包。

## 一、当前闸门状态

截至本手册编写时，项目中没有发现历史行政边界授权包。项目现有地图数据包括：

- Natural Earth 自然地理底图，用于现代地理定位参考；
- 郡国、县治、城邑和关隘等历史地点点位；
- 秦汉时期势力控制、影响、争夺和活动范围的示意面。

这些数据都不等同于历史郡县行政边界。没有通过授权预检前，项目不会下载、抓取、镜像、发布或伪装任何第三方历史行政边界，也不会把 2.0 标为“精确历史行政区地图”。

## 二、Codex 可以独立完成的工作

在不接触受限数据、不替用户作权利承诺的前提下，Codex 可以完成：

1. 读取项目外的授权包并做只读预检；
2. 检查包内目录、文件引用、GeoJSON 结构、基本几何闭合性、坐标范围、时期范围和属性字段；
3. 检查许可证文本是否明确覆盖公开网站展示、静态网站再分发、署名和限制条件；
4. 生成不包含原始受限几何的审校报告；
5. 在授权明确后建立本地预处理管线，包括中国专题范围裁切、统一坐标参考、几何简化、压缩、版本记录和处理说明；
6. 扩展行政地理数据模型、验证器、时期过滤逻辑、缩放级别和地图图例；
7. 将“历史行政区面”和“势力范围示意面”分成不同数据类型、不同图层和不同视觉语义；
8. 增加“行政地理来源与覆盖”说明，并运行类型检查、单元测试、静态构建和浏览器回归检查。

如果授权包包含 Shapefile，Codex 会先检查 `.shp`、`.dbf`、`.shx`、`.prj` 等配套文件；完整几何审校需要可用的 GIS 解析工具。若工具不可用，预检报告会明确将该项标为未完成，不会默认为通过。

## 三、必须由项目所有者完成的事项

以下事项不能由 Codex 代替完成，因为它们需要你与权利人、来源机构或授权记录之间的事实确认：

1. 取得数据权利人或来源机构的许可；
2. 确认许可明确允许公开网站展示；
3. 确认许可明确允许随静态网站发布物再分发，包括处理后的裁切、投影、简化和压缩衍生物；
4. 提供准确的权利人、数据名称、版本、发布日期、来源页面或授权记录地址；
5. 确认署名、版权、引用文字和展示位置要求；
6. 确认是否存在研究用途、个人用途、在线查看、禁止再分发、期限、地域、商业用途或其他限制；
7. 确认每个时期的起止年份、行政层级和要素含义；
8. 确认数据覆盖的区域和年代缺口，不要求项目自动补齐、插值或推断；
9. 在最终发布前，对来源署名和许可证展示文字做最终法律与事实确认；
10. 如果授权包不完整、许可证只允许研究/个人使用、只允许在线查看或禁止再分发，决定将其留在项目外作为本地研究参考，不把它纳入静态站点。

换句话说，Codex 可以审查“你提供的许可文件是否写清楚了这些权限”，但不能替你取得许可、猜测许可范围，或对模糊条款作有利于发布的解释。

## 四、你需要准备的授权包

请在 `D:\warMap` 之外的临时目录，或单独压缩包中准备资料。推荐包名：

```text
han-three-kingdoms-historical-gis-authorized.zip
```

解压后的推荐结构如下：

```text
han-three-kingdoms-historical-gis-authorized/
  LICENSE.md
  MANIFEST.json
  data/
    western-han-xxx-bce/
      boundaries.geojson
    eastern-han-yyy-ce/
      boundaries.shp
      boundaries.dbf
      boundaries.shx
      boundaries.prj
  documentation/
    citation-or-source-note.pdf
```

也可以使用 `source-notes.md` 代替 PDF。原始资料不要直接复制到项目的 `data/`、`public/` 或其他构建目录中；交接阶段只需要把包放在项目外，并把路径告诉我。

### LICENSE.md 必须回答的问题

许可证文本或授权说明必须清楚写明：

- 数据权利人或来源机构是谁；
- 数据名称、版本和发布日期是什么；
- 你取得的许可范围是什么；
- 是否明确允许公开网站展示；
- 是否明确允许随静态网站再分发；
- 必须保留的署名、版权和引用文字是什么；
- 是否有禁止用途、期限、地域、商业用途或其他限制。

仅写“可用于研究”“可在线查看”“个人使用”或“来源公开”不能视为允许静态网站再分发。许可证没有明确写出再分发权限时，闸门保持关闭。

### MANIFEST.json 最小格式

每个时期至少应有以下信息：

```json
{
  "datasetId": "example-han-commandery-boundaries",
  "version": "x.y",
  "releaseDate": "YYYY-MM-DD",
  "license": "明确的展示与再分发许可说明",
  "sourceUrl": "来源页面或授权记录地址",
  "periods": [
    {
      "id": "western-han-xxx-bce",
      "startYear": -202,
      "endYear": -141,
      "file": "data/western-han-xxx-bce/boundaries.geojson",
      "featureMeaning": "commandery",
      "level": "commandery",
      "coverage": "覆盖区域和年代缺口说明",
      "citation": "要求展示的引文"
    }
  ]
}
```

`featureMeaning` 必须指向一种明确的行政实体，例如 `commandery`、`kingdom`、`province` 或 `county`。不能填写 `influence`、`control-area`、`schematic-territory`、`activity-range` 等势力范围语义，也不能把多种层级混在一个时期文件里而不说明。

## 五、交付步骤

1. 从权利人或来源机构取得授权文件和数据文件。
2. 按第四节的结构整理包，保留原始文件名和原始版本信息。
3. 自查许可证是否明确允许“公开展示”和“静态网站再分发”。
4. 自查每个时期的年份、层级、要素含义、覆盖范围和引文。
5. 将压缩包或解压目录放到 `D:\warMap` 外。
6. 告诉我包的绝对路径，例如：

   ```text
   D:\historical-gis\han-three-kingdoms-historical-gis-authorized
   ```

7. 我会先运行只读预检，并给出通过、警告和阻断项；在阻断项清零前不会把数据放入构建资源。
8. 预检通过后，我会建立处理产物目录、生成处理元数据，再接入应用和发布说明。

## 六、预检命令

项目已提供只读预检脚本。它不会复制原始数据、修改授权包或发布任何文件：

```powershell
npm run preflight:historical-gis -- "D:\historical-gis\han-three-kingdoms-historical-gis-authorized"
```

如需把不含原始几何的报告保存到指定位置：

```powershell
npm run preflight:historical-gis -- `
  "D:\historical-gis\han-three-kingdoms-historical-gis-authorized" `
  --report "D:\historical-gis\v1.9.7-preflight-report.json"
```

报告会记录文件完整性、许可字段、时期元数据、几何检查摘要、覆盖范围摘要、警告和阻断项，不会把原始坐标或受限数据复制到报告中。

## 七、什么情况下会停止

出现以下任一情况，行政 GIS 接入会停在授权闸门处：

- 缺少 `LICENSE.md` 或 `MANIFEST.json`；
- 许可证没有明确允许公开网站展示；
- 许可证没有明确允许静态网站再分发；
- 许可证明确禁止再分发，或只允许研究、个人使用或在线查看；
- 数据来源、版本、发布日期或引用要求不清楚；
- 时期起止年份、行政层级或要素含义不清楚；
- 文件缺失、GeoJSON 无法解析、几何明显无效或 Shapefile 配套文件不完整；
- 数据实际是势力范围、活动区或手绘示意面，却被声明为行政边界；
- 覆盖缺口被要求通过自动插值、现代边界替代或主观绘制补齐。

停止并不妨碍继续完善不依赖历史边界的项目功能；但受限数据不会进入静态网站构建物。

## 八、最终发布前的用户确认清单

在我完成技术接入后，发布前请你逐项确认：

- [ ] 每个进入发布物的时期文件都对应明确许可。
- [ ] 许可允许公开网站展示和静态网站再分发。
- [ ] 页面显示了要求的权利人、署名和引用文字。
- [ ] 页面列出每个时期、层级、版本、许可和限制。
- [ ] 行政区面与势力范围示意面使用不同图例和视觉语义。
- [ ] 年份切换不会显示不适用时期的数据。
- [ ] 未覆盖年代和区域明确显示缺口，没有自动插值或伪造。
- [ ] 总览、放大层级、标签密度和战争/路线/水系遮挡情况经过验收。
- [ ] 你已对许可事实和最终署名文字完成最终确认。

## 九、当前项目的明确表述

在授权包通过前，项目只能表述为“带有历史地点、自然地理参考底图和势力范围示意的秦汉战争地图”，不能表述为“精确历史行政区地图”。现有势力面继续保留为势力范围示意，不会被重命名为郡县边界。
